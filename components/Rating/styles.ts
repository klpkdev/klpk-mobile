import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerRating: {
    marginTop: 22,
  },
  ratingGroup: {
    flexDirection: 'row',
    gap: 10,
  },
  star: {
    marginHorizontal: -7,
  },
});

export default styles;
